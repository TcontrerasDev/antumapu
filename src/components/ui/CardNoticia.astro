---
interface Props {
    url: URL | string;
    imgSrc: string;
    fecha: string;
    titulo: string;
    descripcion: string;
    theme?: "light" | "dark";
}

const { url, imgSrc, fecha, titulo, descripcion, theme = "light" } = Astro.props;
---

<article class="col-span-full lg:col-span-4 hover:[&_h3]:underline">
    <a href={url} class="flex flex-col gap-5">
        <div class="h-80">
            <img src={imgSrc} alt={titulo} class="w-full h-full object-cover" />
        </div>
        <div class="flex flex-col gap-1">
            <span class="uppercase text-gold-700 text-base font-heading-2"
                >{fecha}</span
            >
            <h3
                class:list={[
                    "text-h3",
                    {
                        "text-navy-950": theme === "light",
                        "text-sand-100": theme === "dark",
                    },
                ]}
            >
                {titulo}
            </h3>
            <div
                class:list={[
                    "text-base",
                    {
                        "text-navy-950": theme === "light",
                        "text-white-soft": theme === "dark",
                    },
                ]}

                set:html={descripcion}
            >
            </div>
        </div>
    </a>
</article>
