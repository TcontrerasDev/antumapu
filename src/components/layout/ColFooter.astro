---
interface Props {
	title: string;
}

const { title } = Astro.props;
---

<div class="col-span-12 lg:col-span-4 flex flex-col gap-5 mb-5">
    { title && <h3 class="text-2xl font-bold uppercase tracking-[1.4px] text-gold-500">{title}</h3> }
    <slot />
</div>